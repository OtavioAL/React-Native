import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textTitle: {
    fontSize: 14,
    fontWeight: "400",
    marginBottom: 8,
  },
  containerRow: {
    flexDirection: "row",
    gap: 12,
  },
});
